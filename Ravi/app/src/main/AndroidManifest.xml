<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.grofers_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GrofersApp">
        <activity android:name=".PaymentOptionActivity"></activity>
        <activity android:name=".ProductCart_Activity" />
        <activity android:name=".SavedWhereYouWantAddress" />
        <activity android:name=".ProductDetailsActivity" />
        <activity android:name=".DiscountFragments.DicountMainActivity" />
        <activity android:name=".Home_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>